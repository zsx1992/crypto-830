# crypto-830 推送稳定性诊断报告

日期：2026-09-10
范围：企微推送链路（scanner 六闸过滤 → 去重 → 渲染 → webhook）
样本：state.pushedSignals 268 条、observedSignals 25 条、4 个 run 的完整漏斗日志

---

## 一、一句话结论

两个极端不是两个 bug，而是**同一套规则的两种失效方向**：

> 推送触发条件被设计成「发现一个新形态」这一个**事件**，
> 但形态本身是「持续存在好几天」的**状态**。

于是推送量 ≈ 单位时间「新形态诞生率」，而这个率由市场驱动、跨标的高度相关：

- 市场一动 → 几十个标的同时出形态 → 暴推（09-04 单日 111 条）
- 市场一静 → 几乎无新形态 → 归零（09-06 / 09-07 各 1 条，09-10 至今 0）

而为了压住「暴推」叠加的一层层闸（freshness / 同形态去重 / 确认容差），
又把安静期的量进一步压到 0，同时把「故意推的边界样本」混进了同一个群。

---

## 二、现状数据

### 2.1 每日推送量（UTC）

| 日期 | 条数 | 主要成分 |
|---|---|---|
| 09-03 | 50 | double_bottom 36、H&S bottom 8 |
| 09-04 | **111** | **H&S top 61**、double_bottom 27、double_top 15 |
| 09-05 | 45 | H&S top 29、double_bottom 12 |
| 09-06 | **1** | H&S bottom 1 |
| 09-07 | **1** | double_bottom 1 |
| 09-08 | 49 | descending_channel 18、rectangle 13 |
| 09-09 | 11 | 混合 |
| 09-10 | **0**（至今） | — |

强度分全部 ≥55（min 56~62），说明 **min_strength 不是零推送的原因**。

### 2.2 扫描漏斗（4 个 run 交叉验证，结论完全一致）

| run | 候选 | 确认 | freshness 砍 | 其他闸砍 | 过滤后 | 去重后 | 推送 |
|---|---|---|---|---|---|---|---|
| 34423538900（09-10 01:08） | 280 | 43 | **29** | 5 | 9 | **0** | 0 |
| 34422025508（09-10 00:36） | 278 | 44 | **30** | 5 | 9 | **0** | 0 |
| 34415090806（09-09 23:02） | 348 | 57 | **37** | 7 | 13 | 1 | 1 |
| 34402423834（09-09 20:40） | 338 | 57 | **40** | 4 | 13 | 1 | 1 |

两处断崖：
1. **freshness 闸砍掉 65~70% 的确认信号**
2. **去重「同形态已推过」砍掉幸存者的 85~100%**

注：`冷却期内` 拦截数在 4 个 run 中**全部为 0** —— 冷却期（15m/1h/4h/1d = 30/120/480/1440 分钟）
**不是**零推送的原因，真正的杀手是 `same_form`（同形态永久抑制）。

### 2.3 观察流占比

| 日期 | 正式推送 | 观察流推送 |
|---|---|---|
| 09-08 | 49 | 13 |
| 09-09 | **11** | **12** |

**09-09 观察流推送数（12）超过了正式推送数（11）** —— 群里一半以上的消息
是「故意推的、不达标的边界样本」。

---

## 三、零推送根因

### 3.1 freshness 闸：砍 65~70%

检测器扫描整段历史，找到的「已确认形态」里大部分突破发生在很久以前。
freshness 窗口（15m 30 / 1h 48 / 4h 24 / 1d 22 根）只放行最近的一批。

### 3.2 同形态永久抑制：砍 100%（真正的最后一刀）

`src/state_store.py::_suppressed`：

```
命中 signalHash(symbol|patternType|interval)
  ├─ 方向翻转        → 放行（结构已破坏，允许重推）
  ├─ end_ms 相同(±2根) → 永久抑制   ← 无时间上限
  └─ 否则            → 冷却期兜底
```

`end_ms` = 形态最后一个 pivot 对应 K 线的 openTime。
形态只要还挂在图上没被破坏，`end_ms` 就**一直不变** → 每轮都被判「同形态已推过」。

run#224 实测 9 个幸存者**全部**是昨天已推过的同一形态，`端差 0ms`：

```
GLWUSDT double_bottom 4h / PNUTUSDT double_bottom 1d / XIAOMIUSDT double_top 4h
RAYUSDT double_bottom 1d / ZHIPUUSDT head_shoulders_top 4h / AVAXUSDT H&S bottom 1d
AAVEUSDT H&S bottom 1d / ETHUSDT rectangle 15m / HBARUSDT double_bottom 1d
BNBUSDT double_top 1h / TURBOUSDT double_bottom 1d / GOOGLUSDT descending_triangle 1d
```

### 3.3 结构性后果

每个形态一生只推一次；形态在图上停留数天，期间持续占用「新鲜」名额并被去重。
**新推送量 = 新形态诞生率**，安静期必然 0。

### 3.4 历史佐证：这是复发

git 记录 `436fb53 修复根因: 几天0推送 —— freshness窗口过窄 + strength/rr/geo三闸过严`
—— 同一症状此前发生过一次，靠放宽阈值修好；之后为消除伪形态又逐步收紧，
于是再次复发。**系统在「暴推」与「零推」之间来回震荡**，是调参式迭代的必然结果。

---

## 四、乱推送根因

### 4.1 观察流与正式推送同群（最主要、最直接）

`src/scanner.py` 观察流推送与正式推送**共用同一个 `WECOM_WEBHOOK`**。

观察流的设计目的是「故意推送穿过 freshness 但被后续闸砍的边界样本，供人眼验证攒金标准」，
它推送的内容**天然不标准**。用户在同一群里看到，自然会判为「乱七八糟」。

用户此前点评的三张图全部来自观察流：
- `MINIMAXUSDT 15m H&S底` →「什么也不是简直瞎画」
- `ZBTUSDT 4h` →「算是吧」
- `TURBOUSDT 1d 双底` →「看着也还行」

### 4.2 摘要噪声淹没真信号

`notification.send_summary: true` —— **每轮扫描都推一条摘要**（每小时 3 个触发点）。
实测近期约 13~20 个 run/天，即 **15~20 条摘要/天 vs 0~1 条真信号/天**，信噪比约 15:1。
真信号被淹没在「扫描完成 / 漏斗 / 推送:0」的刷屏里。

### 4.3 确认容差 > 几何评分归零线（已修 5 处，仍有残留）

同一类错位：「检测器认为算形态，画出来评分判 0 分」。

| 项 | 修前 | 修后 | 状态 |
|---|---|---|---|
| 头肩 颈线 | 0.08 | 0.05 | ✅ db2f685 |
| 头肩 肩对称 | 0.08 | 0.05 | ✅ a32f7fc |
| 头肩 头部突出 | 0.01 | 0.03 | ✅ a32f7fc |
| triangle 价格包住 | 缺失 | 8%/15%/15% | ✅ 71422ff |
| box 方向+宽度 | 缺失 | 已加 | ✅ 8968cc6 |
| **box 矩形水平度** | **每根 0.0004** | **未修** | ❌ **残留** |

残留项量化：全池 26 个「矩形」里，边界累计抬升 ÷ 箱高 ≤10% 的**只有 2 个**，
其余 24 个 >30%（XAU 300% / LINK 228% / INTC 188%）。**92% 的矩形推送其实是斜的。**

### 4.4 暴推日的低质洪峰

09-04 单日 111 条（H&S top 61 条）是精度收紧前的老代码产物。
这类洪峰既是「不相关」的主体，也是后续被去重反噬、造成零推送的原因。

---

## 五、推送命中的判定条件（显式分层清单）

一条信号要真正推出去，必须逐层通过：

| 层 | 条件 | 当前取值 | 位置 |
|---|---|---|---|
| L0 数据 | 标的池成交额门槛 | core ≥500万U（top 300）/ tail ≥100万U（300） | config `market` |
| L0 | 周期覆盖 | core 全周期；tail 只大周期 | config `tail_intervals` |
| L0 | K 线健康度 | healthy 696 / sensitive 60 / over_smoothed 1 | 日志 health_stats |
| L0 | 噪音过滤 | ATR/Price ≥0.5%、单根振幅 ≤8%、量比 ≥0.3 | config `noise` |
| L1 形态 | 检测器几何命中 | 候选 280 | detector |
| L2 确认 | 突破确认 + 交叉确认 + 突破仍有效 + 双顶回踩窗口 | 确认 43 | detector / `require_intact_breakout` |
| **L3 闸1** | **突破年龄 ≤ 窗口** | **15m 30 / 1h 48 / 4h 24 / 1d 22 根** | **`freshness_bars`** |
| L3 闸2 | 强度分 ≥55 | 55 | `min_strength` |
| L3 闸3 | 盈亏比 ≥0.5 | 0.5 | `min_rr` |
| L3 闸4 | 量比 ≥1.5 | 1.5 | `min_volume_ratio` |
| L3 闸5 | 几何分 ≥0.45 | 0.45 | `min_geometry` |
| L3 闸6 | ADX≥20 时方向与趋势同向 | 20 | `require_trend_alignment` |
| L3 限量 | 每标的每轮最多 2 条 | 2 | `max_per_symbol` |
| **L4 去重** | **同 signalHash + 同 end_ms → 永久抑制** | **无上限** | **`_suppressed`** |
| L4 去重 | 否则冷却期 | 30/120/480/1440 分钟 | `cooldown_minutes` |
| L5 排序 | 按 strength → rr 排序取前 N | — | `scanner` |
| L6 推送 | 渲染图 + webhook 成功 | 限流 18 条/分钟 | `notifier` |

**当前命中的瓶颈在 L3 闸1（freshness）与 L4（同形态永久抑制），不是 L3 闸2~6。**

---

## 六、优化方案

### 动作 A：观察流物理隔离 —— 治「乱」，成本最低

- 观察流改用**独立 webhook**（另建一个企微群 / 群机器人），或降级为「每日一条汇总」。
- 效果：群里只剩达标信号，立刻消除「内容乱七八糟」的感知。
- 风险：需新增一个 Secrets；不影响任何检测逻辑。

### 动作 B：去重模型从「永久抑制」改为「状态跃迁可重推」 —— 治「零」，核心改动

同一形态在生命周期内允许推 **多个事件**，每次跃迁是一个新信号：

```
形态确认（首次）→ 突破发生 → 回踩确认 → 形态失效（预警）
```

- 实现：`signalHash` 增加状态后缀（或记录 `lastPushedStage`），
  `same_form` 只在「同一阶段 + 同 end_ms」时抑制。
- 效果：单个形态的可推事件数从 1 提升到 1~3，推送量更平滑且每个事件都有信息量。
- 风险：**改动最大**，需定义状态机；形态类型/哈希变化会重置去重，**可能一次性推积压**
  （历史上 box 首次上线 run#186 一次推了 18 条）→ 需配推送限速。

### 动作 C：freshness 从硬阈值改为「新鲜度加权」 —— 治「零」，中等

- 不再一刀切「超窗即砍」，而是按突破距今给新鲜度分，并入 strength 综合排序。
- 老突破不占「入场信号」位，但可转为「形态仍在」的提示。
- 风险：会放行部分较老信号，需配合文案区分（避免又变成推陈旧形态）。

### 动作 D：扩大标的池 / 增加周期 —— 提高基数

- 现状 core 300 + tail 300 只产出 280 候选。提高 `top_n` 或给 tail 补小周期可线性提升新形态率。
- 风险：单次扫描已 7~9 分钟，扩池会拉长 Actions 时长。

### 动作 E：矩形水平度收尾 —— 治「乱」

- 把 box 的「平」判定从「每根 rel_slope ≤0.0004」改为「边界累计抬升 ÷ 箱高 ≤15%」，
  通道判定改用边界方向（方案已评估：26 → 2 rectangle / 14 ascending_channel / 8 descending_channel / 2 拒绝）。
- 风险：形态类型变化 → signalHash 变化 → 去重失效 → 可能推送洪峰，需配限速。

### 动作 F：摘要降噪 —— 治「乱」

- `send_summary` 改为**仅在有信号或异常时推送**，或降为每日一条。
- 效果：信噪比从 ~15:1 提升到接近 1:1。

### 推荐组合

| 优先级 | 动作 | 理由 |
|---|---|---|
| P0 | **A + F** | 零逻辑风险，立刻消除「乱」的感知 |
| P1 | **E** | 已评估完成，收尾精度 |
| P2 | **B** | 治本，但需设计状态机 + 限速，建议单独一轮 |
| P3 | C / D | 视 B 落地后的量再决定 |

---

## 七、边界与诚实交代

1. **「稳定发出」有物理上限。** 形态诞生是市场驱动的脉冲事件，
   不可能也不应该让推送变成每天固定条数。硬凑数量必然重新引入伪形态。
   可追求的是「**平滑**」而非「**恒定**」：靠事件化（B）+ 分级（A）把脉冲摊平。
2. **安静期 0 推送本身是正确的。** 此时市场确实没有合格的、新鲜的、标准的形态，
   推任何东西都是噪声。要区分「系统故障导致 0」与「市场确实没有」——
   本报告已定位当前属于后者叠加了去重过严。
3. **本报告只做诊断，未改动任何生产代码。** 所有改动待确认后执行。
4. **未覆盖**：企微 webhook 的实际投递成功率（需要 webhook 侧回执，当前只有本地成功计数）；
   GitHub schedule 被跳过的比例（已知问题，靠三触发点冗余缓解）。
